#include <bits/stdc++.h>
using namespace std;
int main(){
    int length;
    cin >> length;
    int n,num,sum = 0; ;
    cin >> n;
    for(int i = 0; i < n;i++){
        cin >> num;
    
    }
    if(sum <= length) cout << "YES";
    cout << "NO";
}